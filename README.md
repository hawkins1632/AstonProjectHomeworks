# Консольное CRUD приложение (User-Service) — Командный проект для обучения Hibernate.

## Состав команды и распределение задач
Морозов Павел (Team Lead на это задание)
* Настройка общего репозитория GitHub, правил защиты веток (Rulesets).
* Задача 1: Создание инфраструктуры проекта (`pom.xml`, `hibernate.cfg.xml`, `HibernateUtil.java`, `logback.xml`).
* Задача 3: Реализация методов изменения данных в DAO (`save`, `update`, `delete`) с поддержкой транзакций.
* Задача 5: Разработка консольного интерфейса (`Main.java`) с обработкой ввода.

Мизина Диана 
* Задача 2: Создание базовой сущности `User.java` с JPA-аннотациями и триггером `@PrePersist`.
* Задача 4: Реализация методов чтения данных в DAO (`findById`, `findAll`).


## Техническое задание
Реализовать консольное приложение для управления пользователями с использованием Hibernate ORM и СУБД PostgreSQL. 
Код должен быть покрыт Javadoc-комментариями. 
Разработка ведется строго через ветку `develop` и Pull Request'ы с обязательным код-ревью.

## Стратегия слияния веток (Git Strategy)
Команда утвердила стратегию **Merge commit** для сохранения прозрачной истории интеграции фич.

